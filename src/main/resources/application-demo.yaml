spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    initialization-mode: ALWAYS
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

  profiles:
    include: github